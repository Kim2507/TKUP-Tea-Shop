package com.capstone.TeaShop.controller;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PostMapping;

import com.capstone.TeaShop.model.User;

@Controller
public class RegisterController {
	/*
	 * @GetMapping("/register") public String showForm(Model model) { Account user =
	 * new Account(); model.addAttribute("user",user);
	 * 
	 * return "registration"; }
	 * 
	 * @PostMapping("/register") public String submitForm(@ModelAttribute("user")
	 * Account user) { System.out.println(user); return "register_success"; }
	 */

}
